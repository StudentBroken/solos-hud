import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // Tests will be added once BLE and GPS are mockable
    expect(true, isTrue);
  });
}
